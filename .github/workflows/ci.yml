name: CI Pipeline

on:
  push:
    branches: [ main ]

jobs:
  check-project:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check files
        run: |
          echo "Checking project..."
          ls
          test -f index.html
          echo "All good!"